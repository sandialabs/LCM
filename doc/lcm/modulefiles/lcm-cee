#%Module 1.0

module-whatis "LCM CEE LAN (hpws) base environment using SEMS v2 modules"

setenv LCM_ENV_TYPE CEE

# SEMS v2 modules require Lmod (for .lua module files).
# The user must source the SEMS init script BEFORE loading LCM modules:
#   source /projects/sems/modulefiles/utils/sems-modules-init.sh
# This replaces the Tcl Environment Modules with Lmod and sets up
# hierarchical SEMS module paths (compilers -> MPI -> TPLs).

module load lcm-common

conflict lcm-fedora lcm-rhel lcm-ubuntu
